#stop the program from commiting the node modules in the main directory for nodejs and expressjs
/node_modules

#stop the program from commiting the node modules in the client directory for angular 4
/client/node_modules

#stop the program from commiting assets folder on server for testing purposes
/assets

#stop the program from commiting ssh keys folder
/ssh

#ignore the client folder in production mode
/client

#ignore the api until updates
/api